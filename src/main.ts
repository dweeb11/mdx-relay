import { Plugin } from "obsidian";

export default class MdxRelayPlugin extends Plugin {
  override onload(): void {
    // Product behavior is intentionally introduced by later approved tasks.
  }
}
